#include "main.h"
/**
* _abs - computes the absolute value of an integer
* @n: the integer to be computed
*
* Return: return the absolute value of the given integer
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
